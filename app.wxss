@import './styles/iconfont.wxss';

/* 微信小程序中不支持通配符  '*'  */
page,
view,
text,
swiper,
swiper-item,
image,
navigator {
	padding: 0;
	margin: 0;
	box-sizing: border-box;
}

page {
	/* 定义主题颜色 */
	--themeColor: #eb4450;
	/* 统一字体大小 设计稿 375px */
	font-size: 28rpx;
}

image {
	width: 100%;
}
